import { ProcessedCategoriesTextPipe } from './processed-categories-text-pipe';

describe('ProcessedCategoriesTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessedCategoriesTextPipe();
    expect(pipe).toBeTruthy();
  });
});
