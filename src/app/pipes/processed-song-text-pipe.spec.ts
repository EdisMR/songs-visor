import { ProcessedSongTextPipe } from './processed-song-text-pipe';

describe('ProcessedSongTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessedSongTextPipe();
    expect(pipe).toBeTruthy();
  });
});
